const firstName = "Emanuel";
const lastName = "Sobral";

const name = firstName + lastName;

function addFirstLastName(firstName, lastName) {
  return firstName + lastName;
}

const firstNames = ["Emanuel", "Nemi"];
const lastNames = ["Sobral", "Thapa"];

const person1 = {
  firstName: "Emanuel",
  lastName: "Sobral",
  age: 28,
};

const person2 = {
  firstName: "Nemi",
  lastName: "Thapa",
  age: 27,
};

function getAge(person) {
  return person.age;
}
